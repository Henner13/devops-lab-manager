# ansible.cfg

[defaults]
inventory = inventory/hosts.ini
roles_path = ansible/roles
host_key_checking = False
forks = 10 #número máximo de hots que se gestionarán en paralelo
vault_password_file = .vault_pass

[privilege_escalation]
become = True
become_method = sudo
become_user = root
